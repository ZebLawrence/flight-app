## Task 0.1.2 — Add Docker Compose for dev environment

**Status**: ✅ COMPLETE

**What was done**:
- Created docker-compose.yml with PostgreSQL 16 container configuration
  - Container name: flight-app-db
  - Port: 5432
  - Credentials: postgres/postgres
  - Database: flightapp
  - Volume: postgres_data for persistence
- Created .env.example with DATABASE_URL connection string
- Updated .gitignore to exclude .env and .env*.local files

**QA Note**: Docker is not installed on this development machine. The configuration is complete and ready to test once Docker is installed. To verify: `docker compose up db` then `psql -h localhost -U postgres -d flightapp`

**Next task**: Task 0.1.3 — Install and configure Drizzle ORM (depends on 0.1.2)

---
