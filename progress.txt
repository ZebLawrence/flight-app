## Task 0.2.3 — Run first migration

**Status**: in-progress

**Why this task**: Tenants and pages schemas are ready; migration can now be generated and applied.

**What we're doing**:
- Run `drizzle-kit generate`
- Run `drizzle-kit migrate`
- Verify `tenants` and `pages` tables exist in Postgres

**Expected outcome**: DB schema is materialized in PostgreSQL and visible via table listing.

---
